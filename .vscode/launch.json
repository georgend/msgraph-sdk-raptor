{
    "version": "0.2.0",
    "configurations": [
        {
            "request": "attach",
            "type": "coreclr",
            "processId": "${command:pickProcess}",
            "name": ".NET Attach"
        }
    ]
}